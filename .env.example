# ============================================================
# CYH Mapping — Environment Variables
# ============================================================
# Copy this file to .env.local (for CRA) or .env (for Vite)
# NEVER commit real values to the repo.
# ============================================================

# Backend API base URL (no trailing slash)
# CRA prefix: REACT_APP_   →   After Vite migration: VITE_
REACT_APP_API_URL=http://localhost:5050/api

# Your domain (used for share links, etc.)
REACT_APP_SITE_URL=http://localhost:3000

# Google Analytics Measurement ID (leave blank to disable)
REACT_APP_GA_ID=
